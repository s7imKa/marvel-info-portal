import error from './error.png'
import './error.css'

const Error = () => {
   return (
      <>
         <img className={'img-error'}src={error} alt='error' />
      </>
   );
};

export default Error;

